{%- parse_json categories_map -%}
{
  "how-platformos-works": "How platformOS Works",
  "get-started": "Get Started",
  "tutorials": "Tutorials",
  "api-reference": "API Reference",
  "use-cases": "Use Cases",
  "best-practices": "Best Practices",
  "contributor-guide": "Contributor Guide"
}
{%- endparse_json -%}

{%- assign default_name = context.params.slug | titleize -%}
{%- assign slug = context.params.slug -%}
{%- assign name = categories_map | fetch: slug -%}

{% if name %}
{%- export slug, name, default_name, namespace: 'category' -%}
{% endif %}
---
layout_name: application
converter: markdown
metadata:
  title: pos-cli
  description:
---

The **[pos-cli](https://www.npmjs.com/package/@platformos/pos-cli)** is a command line interface that helps you deploy configuration files and assets to your platformOS site.

## Requirements

pos-cli requires [Node.js](https://nodejs.org/en/download/) in at least latest LTS version to work properly.

## Installation

Follow our [Installing the pos-cli](/get-started/hello-world/install-pos-cli) tutorial to install the pos-cli.

## Commands and options

All commands should be run in the project root directory (one level above the `app` directory).

You can learn about all the commands in [the readme](https://www.npmjs.com/package/@platformos/pos-cli).

<% if object.has_tag?('return') %>
### Returns:
<% object.tags('return').each do |tag| %>
<%= format_types(tag.types) %> - <%= tag.text %>
<% end %>

<% end %>
<% if object.has_tag?('param') %>
### Params:

<% object.tags('param').each do |tag| %>
* <%= tag.name %> <%= format_types(tag.types) %> - <%= tag.text %> <% if (o=object.parameters.assoc(tag.name.to_s)) && o[1] %> <em class="default">(defaults to: <tt><%= h o[1] %></tt>)</em> <% end %>
<% end %>

<% end %>
<% if object.has_tag?('example') %>
### Examples:
```liquid
<%= object.tags('example').first&.text %>
```

<% end %>

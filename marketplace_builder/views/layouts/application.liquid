<!doctype html>
<html lang="en">
  {% include 'layouts/head' %}
  <body class="{{ context.page.metadata.body_css_class }}" data-version="{{ context.version }}">
    {% include 'layouts/header' %}

    <main class="main">
      {%- include 'layouts/flash-messages', flash: context.flash -%}
      <div class="container">
        <div class="row">
          {% if context.page.metadata.hide_sidebar != true %}
            <aside class="sidebar col-12 col-md-3">
              {% include 'layouts/sidebar' %}
            </aside>
            <section class="content col-12 col-md-9">
          {% else %}
            <section class="content col-12 col-md-12">
          {% endif %}
          {% include 'layouts/content' %}
          {% if page.metadata.exclude_feedback == blank %}
            {% include 'shared/feedback' %}
          {% endif %}
          </section>
        </div>
      </div>
    </main>

    {% include 'layouts/footer' %}
  </body>
</html>

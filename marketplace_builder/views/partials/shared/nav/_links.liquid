<ul>
  {% for link in context.exports.nav.navLinks %}
    <li>
      <a href="{{ link.href }}">{{ link.name }}</a>
      {% if link.children != null %}
        {% include 'shared/nav/links', links: link.children %}
      {% endif %}
    </li>
  {% endfor %}
</ul>
